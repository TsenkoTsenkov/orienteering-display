services:
  - type: web
    name: orienteering-proxy
    runtime: node
    plan: free
    rootDir: proxy-server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production