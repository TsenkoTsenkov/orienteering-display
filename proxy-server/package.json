{
  "name": "liveresults-proxy",
  "version": "1.0.0",
  "description": "Proxy server for LiveResults API to bypass CORS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:simple": "node server-simple.js",
    "build": "npm install",
    "build:full": "npm install && npx puppeteer browsers install chrome",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^24.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
